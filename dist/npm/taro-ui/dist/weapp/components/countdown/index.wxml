<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}">
        <block wx:if="{{isShowDay}}">
            <at-countdown-item compid="{{$compid__96}}"></at-countdown-item>
        </block>
        <block wx:if="{{isShowHour}}">
            <at-countdown-item compid="{{$compid__97}}"></at-countdown-item>
        </block>
        <at-countdown-item compid="{{$compid__98}}"></at-countdown-item>
        <at-countdown-item compid="{{$compid__99}}"></at-countdown-item>
    </view>
</block>